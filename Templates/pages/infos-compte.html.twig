{% extends "base.html.twig" %}

{% block content %}
    <div style="max-width: 500px; margin: 0 auto; padding: 20px;">
        <h1>MON COMPTE</h1>

        {% if user %}
            <div style="background: #e8f5e9; padding: 20px; margin-top: 20px;">
                <p>Bonjour <strong>{{ user.prenom }} {{ user.nom }}</strong> !</p>
                <p>Email: {{ user.email }}</p>
                <p>Âge: {{ user.age }} ans</p>
                <p>Formation: {{ user.formation }}</p>
            </div>
        {% else %}
            <p style="color:red">ERREUR : Veuillez vous <a href="?page=connexion-etu">connecter</a></p>
        {% endif %}
    </div>
{% endblock %}