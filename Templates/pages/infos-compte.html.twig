<!DOCTYPE html>
<html>
<head>
    <title>Mon Compte</title>
    <style>
        body { font-family: Arial; max-width: 500px; margin: 0 auto; padding: 20px; }
        .info { background: #e8f5e9; padding: 20px; margin-top: 20px; }
    </style>
</head>
<body>
<h1>MON COMPTE</h1>

{% if user %}
    <div class="info">
        <p>Bonjour <strong>{{ user.prenom }} {{ user.nom }}</strong> !</p>
        <p>Email: {{ user.email }}</p>
    </div>
{% else %}
    <p style="color:red">ERREUR : Veuillez vous <a href="{{ base_url }}/?page=connexion-etu">connecter</a></p>
{% endif %}
</body>
</html>