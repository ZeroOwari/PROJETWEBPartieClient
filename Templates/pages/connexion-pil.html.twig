{% extends "base.html.twig" %}

{% block head %}
    <script src="{{ asset('connexion.js') }}"></script>
{% endblock %}

{% block content %}
    {% if user %}
    <script>window.location.href = "?page=accueil";</script>
    {% else %}


        <img src="{{ asset('flou.png') }}" class="image">
        <div class="bloc bloc-pink">
            <h2 class="titre-connexion titre-connexion-purple">Se connecter</h2>
            <form class="formulaire-connexion" action="ConnexionPilote.php" method="POST">
                <div class="champ-formulaire champ-formulaire-purple champ-formulaire-connexion">
                    <label for="email">E-mail</label>
                    <input type="email" id="email" name = "email" placeholder="Entrez votre adresse email">
                </div>
                <div class="champ-formulaire champ-formulaire-purple champ-formulaire-connexion">
                    <label for="password">Mot de passe</label>
                    <div style="position: relative;">
                        <input type="password" id="password" name="password" placeholder="Tapez votre mode passe">
                        <button type="button" class="toggle-password" onclick="togglePassword('password')">👁</button>
                    </div>
                </div>
                <div class="option-rester-connecte option-rester-connecte-purple">
                    <input type="checkbox" id="rester-connecte">
                    <label for="rester-connecte">Rester connecter</label>
                </div>
                <button type="submit" id="login-button" class="bouton-connexion bouton-connexion-purple">Se connecter</button>
            </form>
        </div>
        </body>
        </html>

    {% endif %}
{% endblock %}